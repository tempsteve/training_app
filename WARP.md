# WARP.md

這個檔案為 WARP (warp.dev) 提供在此程式碼庫中工作的指引。

## 專案概述

這是一個訓練應用專案（training_app）。專案目前處於初始階段，尚未建立程式碼結構。

## 開發環境設定

專案目前為空的 Git 儲存庫，需要初始化專案結構。

## 常用指令

### 版本控制
```bash
# 查看狀態
git status

# 提交變更
git add .
git commit -m "描述訊息"

# 推送到遠端
git push origin main
```

## 專案架構

專案架構尚未建立，請根據實際開發需求更新此部分。

## 注意事項

- 請在建立專案結構後更新此 WARP.md 檔案
- 加入實際使用的建置、測試、部署指令
- 記錄專案的核心架構和設計模式
